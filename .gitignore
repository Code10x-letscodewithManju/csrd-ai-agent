# --- Python Basics ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# --- Environment Variables (CRITICAL) ---
.env
.env.local

# --- OS Generated Files ---
.DS_Store
Thumbs.db

# --- Project Data Handling ---

# 1. Ignore Raw PDFs (Too large for GitHub, reviewers have them)
data/raw_pdfs/*.pdf

# 2. Ignore Page Index (Intermediate cache, auto-regenerated by script)
data/page_index/

# 3. Ignore Database (Binary file, bad for Git)
data/outputs/*.db
data/outputs/*.sqlite

# 4. KEEP the Final CSV (Proof of work)
# We intentionally do NOT ignore .csv files here so your results are submitted.

# --- Temporary Debug Scripts ---
debug_chain.py
debug_text.py